# 岗位闯关式实训平台 · 学生端前端 PRD

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 岗位闯关式实训平台（学生端） |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 需求来源 | 《岗位闯关式实训平台-需求确认书0706.docx》第 2.3 节「学生端功能」、第 2.1 节「通用功能」、第三章「非功能需求」，以及文档内原型图 image28–image39 |
| 交付范围 | **仅前端**（Vue 3 + Vite）。不包含教师端、不包含后端接口实现，数据先用本地 Mock |
| 本期目标 | 按 P0 优先级把学生端页面全部做出来，可在浏览器中完整走通「选岗位 → 看技能树 → 闯关 → 提交 → 看 AI 判分 → 认证」的主链路 |

---

## 1. 背景与目标

### 1.1 背景

平台面向深圳职业技术大学人工智能本体学院，以「岗位」为导向，把课程实训与技能成长串成一条线，覆盖**岗位认知、项目实训、技能成长、就业认证**四大板块，重点服务工业视觉等专业的实训教学。

传统实训教学的四个痛点，构成了学生端的产品目标：

| 痛点 | 学生端的对应设计 |
| --- | --- |
| 流程不规范 | 每个实训项目拆成固定关卡，逐关提交、逐关通过 |
| 能力难量化 | 技能树把「我会什么」变成可视化的点亮进度 |
| 学习动力弱 | 闯关地图 + 积分 + 等级 + 称号 + 证书，游戏化激励 |
| 教学管理难 | 学生对 AI 判分有异议时可申请教师复评，评价链路闭环 |

### 1.2 学生端要达成的目标

1. **看得懂自己在哪里**：一进首页就知道当前岗位、进度、积分、等级、完成率。
2. **知道下一步做什么**：技能树上未点亮的节点能一键跳到对应实训项目。
3. **能真正练起来**：按七步实训流程逐关完成，提交作答/附件，立刻拿到 AI 判分与改进建议。
4. **留下可展示的凭证**：技能点亮、技能鉴定、认证证书。

### 1.3 非目标（本期不做）

- 教师端全部功能（班级与任务管理、实训项目管理、岗位与技能体系配置、实训结果审核、数据总览、证书管理等）。
- 真实 SSO / JWT / Redis 对接，本期用 Mock 登录与假 Token 代替。
- AI/RAG 能力真实实现，本期用 Mock 回复与 Mock 判分代替，但**接口签名与数据契约按真实能力设计**，后端就绪后可直接替换。
- 电子证书的 PDF 生成与下载（P1/P2，见第 7 节）。

---

## 2. 用户与核心场景

### 2.1 用户角色

| 角色 | 说明 | 本期 |
| --- | --- | --- |
| 学生 | 唯一使用者，通过学校统一身份认证登录，进入学生端 | 本期实现 |
| 教师 | 发布项目、审核报告、复审申诉 | 不做界面，仅通过 Mock 数据体现其产出（已发布项目、教师点评、复审结果） |

### 2.2 核心场景（P0 主链路）

```
登录（SSO 回调）
   └─ 进入【成长中心】：看到我的岗位 / 实训进度 / 快捷入口 + 右侧 AI 助教
        ├─ 点快捷入口「闯关实训」→【关卡地图】：按岗位筛选已发布项目，看关卡节点状态
        │      └─ 点开某个项目 →【关卡详情】：七步关卡逐关作答、上传附件、提交
        │             └─ 提交后弹出【AI 自动评判结果】：总分 + 各维度得分理由 + 提升建议
        │                    └─ 有异议 → 申请教师复评（状态变为「待复审」）
        ├─ 点「技能树」→ 看四大体系节点点亮进度，未解锁节点可「前往实训」
        ├─ 点「岗位选择」→ 看推荐岗位（匹配度排序）→ 确认选择 → 我的岗位同步更新
        └─ 点「认证中心」→ 看认证条件完成情况与已获得证书
```

---

## 3. 信息架构与路由

### 3.1 一级导航（5 个入口）

需求确认书 2.3 节规定的顺序为：成长中心 → 技能树 → 关卡地图 → 岗位选择 → 认证中心。
原型图（image28 / image32 / image33 / image37 / image38）中文案顺序为：成长中心 → 岗位选择 → 关卡地图 → 技能树 → 认证中心。
两者不一致，**暂按文档顺序实现**，并在配置文件中集中定义导航数组，改顺序只需改一处（见第 12 节 Q1）。

| 序号 | 一级入口 | 页面职责 | 优先级 |
| --- | --- | --- | --- |
| 1 | 成长中心 | 我的岗位、实训进度概览、快捷入口 | P0 |
| 2 | 技能树 | 四大体系技能节点树、点亮进度、节点详情 | P0 |
| 3 | 关卡地图 | 已发布实训项目列表、关卡节点与状态 | P0 |
| 4 | 岗位选择 | 岗位推荐、岗位浏览与选择、岗位切换 | P0 |
| 5 | 认证中心 | 认证条件完成情况、已获得/审核中证书 | P0（入口）+ 占位 |

### 3.2 路由表（SPA）

| 路由 | 页面 | 优先级 |
| --- | --- | --- |
| `/login` | 登录中 / SSO 回调页（携带 `?key=xxx`） | P0 |
| `/growth` | 成长中心（默认首页） | P0 |
| `/skill-tree` | 技能树 | P0 |
| `/map` | 关卡地图 | P0 |
| `/map/project/:projectId` | 关卡详情（开始闯关 / 关卡提交） | P0 |
| `/positions` | 岗位选择 | P0 |
| `/certification` | 认证中心 | P0 |
| `/:pathMatch(.*)*` | 404 兜底 | P1 |

规则：

- `/` 重定向到 `/growth`。
- 除 `/login` 外所有路由需登录，未登录跳转 SSO（Mock 环境直接注入 demo 学生身份）。
- 页面组件全部懒加载（`() => import(...)`），导航切换不刷新页面。
- 导航栏高亮态由路由自动匹配（`meta.nav` 字段），子路由（如 `/map/project/1`）高亮父级入口「关卡地图」。

### 3.3 全局框架

所有页面共用一套框架，分四层：

```
┌───────────────────────────────────────────────────────────────┐
│ ① 顶部信息条（蓝色渐变，全宽，高 100px）                        │
│   校徽 · 头像 · 姓名 · 专业 · 班级  …  累计积分 实训等级 完成率 │
│                                      …  消息 待办 收藏          │
├───────────────────────────────────────────────────────────────┤
│ ② 主导航（白底，高 56px，5 个入口，蓝色下划线标识当前页）        │
├───────────────────────────────────────────────────────────────┤
│ ③ 内容区（浅蓝背景 #F0F6FF，卡片式白底内容块，最大宽 1200px）    │
│                                                      ④ 右侧浮层 │
│                                                        AI 助教  │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 全局设计规范

### 4.1 色彩

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 主色 | `#1E7BE8` → `#1677FF` | 顶栏渐变、主按钮、导航高亮 |
| 主色浅底 | `#E8F1FF` | 卡片选中态、标签底 |
| 已完成 | `#52C41A` | 关卡已通过、技能已点亮、成功提示 |
| 进行中 | `#FA8C16` | 当前关卡、进度条、AI 结果卡橙色主题 |
| 可挑战 | `#1677FF` | 已解锁但未开始的关卡 |
| 未解锁 | `#BFBFBF` / `#F5F5F5` | 灰化 + 锁图标 |
| 页面背景 | `#F0F6FF` | 内容区底色 |
| 卡片 | `#FFFFFF` + 圆角 12px + 阴影 | 统一卡片风格 |
| 技能树背景 | `#1E1E32` 深色 | 深色底突出彩色技能节点（原型 image32） |
| 文本 | 主 `#1F2329` / 次 `#646A73` / 弱 `#8F959E` | |

### 4.2 通用组件

| 组件 | 用途 | 出现位置 |
| --- | --- | --- |
| `AppHeader` | 顶部信息条（学生信息 + 三项指标 + 图标入口） | 全局 |
| `AppNav` | 主导航 | 全局 |
| `PageTitle` | 页面标题 + 右侧统计/图例 | 各页 |
| `PositionCard` | 岗位卡片（图标、名称、等级区间、描述、热度、已点亮技能数、难度标签） | 成长中心 / 岗位选择 |
| `SkillNode` | 技能节点（图标、名称、状态、点亮进度角标、锁） | 技能树 |
| `ProjectCard` | 关卡卡片（图标、项目名、分数/状态、进度条、锁） | 关卡地图 |
| `LevelStepper` | 七步关卡步骤条 | 关卡详情 |
| `AiReviewPanel` | AI 自动评判结果面板 | 关卡详情 |
| `AiAssistantDock` | AI 助教对话浮窗（可折叠） | 全局 |
| `EmptyState` | 空态/未解锁引导 | 全局 |

### 4.3 状态与反馈

- 加载态：骨架屏（卡片骨架 / 列表骨架），不用整页 Loading 遮罩。
- 空态：文案 + 引导按钮（如「暂无可挑战项目，先去完成基础实训」）。
- 错误态：局部错误提示 + 重试按钮，页面不白屏。
- 操作反馈：`ElMessage` 轻提示（提交成功、已申请复评等）。确认类操作用 `ElMessageBox`（如「确认选择该岗位？」）。
- 表单校验：必填项未填 / 未上传附件时，提交按钮禁用 + 行内错误提示。

---

## 5. 数据契约（前端类型与 Mock 约定）

前端所有数据经 `src/api/*` 访问，本期由 `src/mock/*` 提供实现。类型定义放 `src/types/`，后端就绪后只替换 `src/api` 内部实现，页面不改。

```ts
// 学生信息（顶部信息条 + 学生信息模块）
interface StudentProfile {
  id: string
  name: string
  avatar: string
  major: string          // 专业，如「人工智能技术应用」
  className: string      // 班级，如「21级2班」
  points: number         // 累计积分
  level: string          // 实训等级，如「高级」
  completionRate: number // 完成率 0-100
  titles: string[]       // 获得称号
}

// 岗位
interface Position {
  id: string
  name: string
  icon: string
  levelFrom: string      // 初级
  levelTo: string        // 中级
  direction: string      // 方向标签（工业视觉 / AI 训练 …）
  difficulty: '较易' | '中等' | '较难'
  description: string
  heat: number           // 热度
  selectedCount: number  // 已选人数 / 通过人数
  litSkillCount: number  // 已点亮技能数
  finishedProjectCount: number
  matchRate: number      // 匹配度 0-100（岗位推荐用）
  selected: boolean      // 是否为当前已选岗位
}

// 技能体系与节点
type SkillSystemId = 'optical' | 'algorithm' | 'deeplearning' | 'deployment'
interface SkillSystem {
  id: SkillSystemId
  name: string           // 光学成像系 / 传统算法系 / 深度学习系 / 系统部署系
  color: string
  nodes: SkillNode[]
}
interface SkillNode {
  id: string
  name: string
  systemId: SkillSystemId
  icon: string
  status: 'locked' | 'active' | 'mastered'  // 未解锁 / 已激活 / 已精通
  progress: { current: number; total: number } // 点亮进度，如 2/3
  description: string    // 简要解锁说明
  prerequisites: string[]// 前置条件（技能名）
  projectIds: string[]   // 关联实训项目，未解锁时「前往实训」跳转用
}

// 实训项目与关卡
type ProjectTier = 'basic' | 'advanced' | 'extended'  // 基础 / 进阶 / 拓展
type ProjectStatus = 'not_started' | 'in_progress' | 'completed' | 'locked'
interface Project {
  id: string
  name: string
  tier: ProjectTier
  icon: string
  positionId: string
  status: ProjectStatus
  levelTotal: number     // 关卡总数
  levelDone: number      // 已完成关卡数
  progress: number       // 0-100
  score?: number         // 已通过时的得分
  lockReason?: string    // 未解锁原因，如「完成更多基础项目即可解锁」
  modules: TrainModule[] // 该项目的关卡配置（默认七大模块）
}
interface TrainModule {
  id: string
  order: number
  name: string           // 需求分析 / 方案设计 / 数据处理 / 模型训练 / 模型优化 / 模型测试 / 报告上传
  required: boolean
  weight: number         // 分值占比
  requirement: string    // 作答要求
  description: string    // 任务描述
  criteria: string[]     // 验收标准
}

// 关卡作答与提交
interface ModuleSubmission {
  moduleId: string
  textAnswers: Record<string, string>  // 按题目 id 存储
  files: UploadFile[]                  // 支持 文本/图片/PDF
  status: 'draft' | 'submitted' | 'passed' | 'rejected'
  submittedAt?: string
  aiReview?: AiReview
}
interface AiReview {
  totalScore: number
  grade: string                        // 优秀 / 良好 / 合格
  dimensions: { name: string; score: number; reason: string; passed: boolean }[]
  suggestion: string
  reviewStatus: 'passed' | 'pending_recheck' | 'rechecked'  // 已通过 / 待复审 / 已复审
}

// 认证
interface Certification {
  id: string
  positionName: string
  icon: string
  status: 'obtained' | 'reviewing' | 'not_met'  // 已获得 / 审核中 / 未达标
  obtainedAt?: string
  conditions: { name: string; required: string; current: string; done: boolean }[]
}

// AI 问答
interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string
  sources?: { title: string; snippet: string }[]  // 知识库来源标注
  createdAt: string
}
```

Mock 要求：

- 至少 6 个岗位、4 大技能体系（每体系 6–9 个节点，含已激活/已精通/未解锁/带点亮进度的混合状态）、9 个实训项目（基础/进阶/拓展各 3 个，含已完成、进行中、可挑战、未解锁四态）、2 张已获得证书、1 个审核中认证。
- Mock 数据集中放 `src/mock/`，用固定 seed，保证每次刷新页面一致，方便对照原型图验收。
- Mock 接口延迟 200–500ms，用于验证骨架屏与加载态。

---

## 6. P0 功能详述

### 6.1 登录与身份（P0，通用功能）

**对应需求**：2.1 用户登录（P0）、角色区分与路由（P0）、SSO 对接（P0）

**页面**：`/login`

**流程（前端视角）**

1. 用户访问平台 → 前端拼接 `backurl` 跳转学校登录页（Mock 环境下展示一个「模拟统一身份认证」按钮）。
2. 校方认证完成后回调 `平台地址?key=Key`，前端读取 `key`。
3. 前端用 `key` 调后端身份查询接口 → 拿到 JWT Token 与身份信息（学号、姓名、角色）。
4. 角色为「学生」→ 写入 Pinia + `localStorage` → 跳转 `/growth`；角色为「教师」→ 提示「请使用教师端」（学生端不做教师页面）。

**实现要求**

- `src/stores/user.ts` 保存 token / profile / role。
- `src/utils/request.ts` 统一注入 `Authorization: Bearer <token>`，401 时清空登录态并跳转登录。
- 路由守卫：未登录访问受保护路由 → 跳转 `/login`；已登录访问 `/login` → 跳转 `/growth`。
- Token 过期自动刷新为 P1，本期先实现 401 兜底跳登录（预留 `refreshToken()` 空实现）。

**验收标准**

- [ ] 带 `?key=` 打开页面能完成「查询身份 → 存 Token → 进入成长中心」。
- [ ] 刷新页面后登录态保持，不重复登录。
- [ ] 手动清空 Token 后访问任意页面都会被引导回登录页。

---

### 6.2 全局框架：顶部信息条 + 主导航（P0）

**对应需求**：学生信息（P0）、主导航（P0）

**顶部信息条**

| 区域 | 内容 |
| --- | --- |
| 左侧 | 校徽 + 学校名（深圳职业技术大学）、学生头像、姓名、专业 · 班级 |
| 右侧指标 | 累计积分（如 3,080）、实训等级（如 高级）、完成率（如 38%） |
| 最右图标 | 消息、待办、收藏（带未读红点数字） |

- 三项指标数值来源于 `StudentProfile`，支持点击「完成率」跳转技能树。
- 获得称号（P0 字段）在头像区悬浮/下拉中展示。

**主导航**

- 五个一级入口按第 3.1 节顺序，含图标 + 文案。
- 当前路由对应项显示主色文字 + 蓝色下划线。
- 配置化：`src/config/nav.ts` 导出 `navItems` 数组，新增岗位/体系等扩展不需改组件。

**验收标准**

- [ ] 五个入口在任意页面都可见，当前页面高亮正确，子页面高亮父级。
- [ ] 点击切换无整页刷新（Network 中无 document 请求）。
- [ ] 顶栏三项指标与 Mock 数据一致。

---

### 6.3 成长中心（P0）

路由：`/growth`。对应原型 image28 / image29 / image31 / image3。

#### 6.3.1 我的岗位（P0，卡片列表）

**注意**：需求文档中该模块有重要修订——**去掉学生的「选择岗位」操作，改为「查看/推荐」**：

- 学生可查看每个岗位需要的技能树，技能树上显示已点亮的技能。
- 岗位顺序按学生**已点亮的技能点数量自动排序**（匹配度高的排前面）。
- 点击未点亮的技能，跳转到对应项目列表（若命中多个项目，按与未点亮技能的匹配度排序）。
- 岗位卡片选中态（蓝框）标识当前目标岗位。

**卡片字段**：岗位图标、岗位名称、等级区间（如 初级 → 中级）、岗位描述（2 行省略）、热度（火焰图标 + 数值）、已点亮技能数（对勾图标 + 「2个」）、难度标签（较易/中等/较难，配色区分）。

**交互**

- 点击卡片 → 打开「岗位技能树」抽屉：展示该岗位所需技能节点，已点亮为彩色，未点亮为灰色且可点击 → 跳转关卡地图并带上岗位筛选与技能参数。
- 卡片显示「匹配度 %」（来自技能点亮匹配度）。

**验收标准**

- [ ] 岗位卡片按点亮技能数倒序排列。
- [ ] 卡片信息字段与原型一致（热度/技能数/难度标签齐全）。
- [ ] 点击未点亮技能能跳到关卡地图，并正确带入岗位与技能筛选参数。

#### 6.3.2 快捷入口（P0）

四个入口卡片，图标 + 主标题 + 副标题，点击跳转：

| 入口 | 副标题 | 跳转 |
| --- | --- | --- |
| 闯关实训 | 开始你的闯关之旅 | `/map` |
| 技能树 | 查看技能成长路径 | `/skill-tree` |
| 认证中心 | 申领企业认证证书 | `/certification` |
| 岗位切换 | 选择其他岗位方向 | `/positions` |

#### 6.3.3 实训进度概览（P1，本轮做静态展示）

三张色块卡片：基础实训 / 进阶实训 / 拓展实训，各显示「已完成项目数 / 总项目数」（如 2/5）。色块底色分别为浅蓝、浅橙、浅紫。P1 阶段再补充点击下钻与趋势图。

#### 6.3.4 AI 助教对话框（P0，见 6.7）

成长中心右侧固定展示 AI 助教浮窗（原型 image3 / image36）。

---

### 6.4 技能树（P0）

路由：`/skill-tree`。对应原型 image32。

**页面结构**

1. 标题「技能树」+ 右上角统计面板（已激活 / 已精通 / 技能总数）。
   > 评审意见（文档批注，作者 longhai）：若按「点亮进度」表达，则这三项统计可以不显示。本期**保留统计面板**，但做成可配置开关（`showSkillStats`），待确认后一键关闭。
2. 主体：深色画布上按 **四大体系分列**（光学成像系、传统算法系、深度学习系、系统部署系），每列自成一个树状结构，列内节点按层级上下排列。
3. 每列底部显示该体系点亮进度：彩色进度条 + 「9 / 9 已激活（100%）」。

**技能节点**

| 状态 | 视觉 | 说明 |
| --- | --- | --- |
| 已激活 | 彩色圆角方块 + 图标 + 右上角 ✓ | 已点亮 |
| 已精通 | 彩色 + 金色描边/星标 | 满级 |
| 点亮中 | 彩色 + 右上角进度角标（如 `2/3`）或 `+` | 需多次达标才点亮 |
| 未解锁 | 灰色方块 + 锁图标 + 灰字 | 前置条件未满足 |

节点尺寸约 68×68，图标下方为节点名称。

**交互**

- 点击任意节点 → 弹出详情卡片：节点名称、所属体系、简要解锁说明、前置条件、当前状态、点亮进度、关联实训项目。
- 状态为「未解锁」时，卡片底部显示「前往实训」按钮 → 跳转 `/map`，并按该节点关联项目排序（命中多个项目时按匹配度排序）。
- 已激活/已精通节点也显示「复习该项目」按钮，可重新挑战。
- 支持体系列横向滚动（体系数量由配置驱动，预留给超级管理员新增体系的扩展点）。

**验收标准**

- [ ] 四大体系分列展示，节点状态与 Mock 数据一致。
- [ ] 带点亮进度的节点显示进度角标。
- [ ] 点击未解锁节点 → 详情卡片 → 「前往实训」能正确跳转并携带参数。
- [ ] 每列底部进度与列内节点状态计算结果一致（自动统计，不写死）。

---

### 6.5 关卡地图（P0）

路由：`/map`。对应原型 image33。

**页面结构**

1. 标题「关卡地图」+ 右上角图例：未解锁（灰）/ 可挑战（蓝）/ 进行中（橙）/ 已完成（绿）。
   > 需求文档原文为三态（已完成绿 / 当前蓝 / 待解锁灰），原型为四态。本期采用**四态**（把「可挑战」从「当前关卡」中拆出），同时保留三态降级映射。
2. 岗位筛选：顶部按岗位类型筛选（下拉或标签组），默认选中学生当前岗位。
3. 项目分组：按层级分成「基础实训 / 进阶实训 / 拓展实训」三个区块，区块头部显示层级名称、副标题（如「新手入门 · 掌握核心基础技能」）与区块进度（如 2/5）。
4. 关卡卡片横向排列（每行 5 个，可横向滚动），卡片内容：
   - 图标（emoji 或图片）
   - 项目名称
   - 状态行：已完成显示「🏆 92分」；进行中显示「▶ 进行中」+ 进度条 45%；可挑战显示「⚡ 可挑战」+ 进度条 0%；未解锁显示「🔒 未解锁」
   - 进度条（按 `progress` 渲染）
   - 未解锁卡片置灰，鼠标悬浮显示提示「完成更多基础项目即可解锁」
5. 点击「可挑战 / 进行中」卡片 → 跳转 `/map/project/:projectId`；点击「已完成」→ 进入详情可查看历史记录与重新挑战（历史记录 P1）；点击未解锁 → 提示解锁条件，不跳转。

**验收标准**

- [ ] 按岗位筛选后列表正确变化。
- [ ] 三个层级区块与进度统计正确。
- [ ] 四种状态的视觉区分明显，未解锁卡片不可进入。
- [ ] 已完成项目显示分数。

---

### 6.6 关卡详情：开始闯关 + 关卡提交 + AI 判分（P0）

路由：`/map/project/:projectId`。对应原型 image34 / image35。

#### 6.6.1 布局

| 区域 | 内容 |
| --- | --- |
| 顶部条 | 「← 返回」+ 项目名称 + 所属层级/岗位 + 右侧「得分」 |
| 步骤条 | 七步关卡：需求分析 → 方案设计 → 数据处理 → 模型训练 → 模型优化 → 模型测试 → 报告上传。当前步骤高亮（主色块），已完成步骤可回看，未解锁步骤灰化不可点 |
| 左侧栏 | 项目名称 + 引导语、任务描述、验收标准（要点列表）、历史记录（时间 + 完成内容）、教师点评 |
| 右侧主区 | 当前关卡的作答区：关卡标题、副标题（本关目标）、若干题目（文本域 + 占位提示 + 说明文案）、附件上传区、底部「提交」按钮 |

关卡顺序与数量由教师端项目配置驱动（默认七步），前端按 `modules` 数组渲染，**不写死七步**。

#### 6.6.2 作答与提交

- 文本作答：`textarea`，展示作答题干与占位示例（如「请描述待检测产品的外观、材质、尺寸等特征」），字数上限（如 2000 字）与字数统计。
- 文件上传：支持**文本、图片、PDF**等格式，单个 ≤ 50MB，支持多文件、预览、删除；上传中显示进度。
- 校验：必填模块未填写或未上传附件时提交按钮禁用，并给出行内提示。
- 草稿：切关卡或离开页面时提示保存草稿（本期用 Pinia + localStorage 暂存）。
- 提交后：调用 Mock 提交接口 → 显示判分中动效（AI 判分 1–2 秒）→ 弹出 AI 评判结果。
- 前置关卡未通过时，后续关卡不可进入（步骤条灰化 + 提示「先通过上一关」）。

#### 6.6.3 AI 自动评判结果（P0）

橙色主题结果面板（原型 image35）：

- 圆形总分（如 91）+ 评判等级（优秀）+ 说明「AI 已完成本次实训的自动评判」。
- 各维度评分条目：维度名 + 通过图标 + 评审理由（维度名由项目评分标准配置驱动，前端按数组渲染）。
- 「AI 提升建议」区块：1–3 条改进建议。
- 底部按钮：「返回关卡地图」+「申请教师复评」。

状态流转（与教师端审核列表对齐）：

```
提交 → AI 自动评审 → 默认「已通过」
                          └─ 学生对结果有异议 → 申请教师复评 → 「待复审」
                                 └─ 教师保存复审 → 「已复审」→ 结果（教师分数与批注）回流学生端展示
```

- 申请复评需二次确认，可在结果面板中填写异议说明（选填，≤200 字）。
- 「已复审」后，结果面板同时展示 AI 分数与教师复审分数、教师批注。
- 通过后：当前关卡标记完成，下一关解锁（步骤条更新 + 轻提示「关卡已通过，下一关已解锁」）。

**验收标准**

- [ ] 七步步骤条状态正确（已完成/当前/未解锁）。
- [ ] 文本 + 附件提交后能拿到 AI 判分结果并展示完整维度理由与建议。
- [ ] 通过后下一关解锁，未通过时停留在本关。
- [ ] 申请复评后状态变为「待复审」，重进页面仍保持。
- [ ] 左侧历史记录与教师点评随提交更新。

---

### 6.7 AI 智能问答 / AI 助教（P0）

对应需求：AI 智能问答 · 课程知识问答（P0）。对应原型 image36、image3 右侧浮窗。

**形态**：全局可用的对话浮窗（成长中心常驻右侧；其他页面以右下角悬浮球展开）。标题「AI 助教对话框」+ 在线绿点。

**功能要求**

1. 首次进入展示欢迎语与当前上下文提示（如「当前你正在进行『亚克力板划痕检测』实训，需要帮助吗？」）——上下文随当前页面/项目自动变化。
2. 输入问题 → 发送 → 展示 AI 回复；回复需**标注信息来源**（知识库片段标题/来源），体现 RAG 检索增强生成。
3. 限制规则（需求文档明确要求，前端需落地）：
   - 学生问答**长度限制**（如单条 ≤ 500 字，超出禁用发送并提示）。
   - **Token 消耗限制**：单次会话与每日次数上限，接近上限时提示。
   - **旧对话存储限制**：仅保留一周内的历史对话。
   - **上下文限制**：同一次对话仅保留前后三次问答上下文。
4. 快捷提问：空态提供 2–4 个推荐问题气泡。
5. 交互细节：Enter 发送 / Shift+Enter 换行；发送中禁用输入并显示「对方正在输入」；回复失败可重试；支持清空对话。

**验收标准**

- [ ] 浮窗可展开/收起，不遮挡主要操作区。
- [ ] 提问后返回带来源标注的回复。
- [ ] 超长输入、超限提示按规则生效。
- [ ] 对话上下文只带最近三轮（可在 Mock 请求日志中验证）。

---

### 6.8 岗位选择（P0）

路由：`/positions`。对应原型 image37，以及需求文档 2.3 节「岗位推荐与选择」。

**前置条件**：须先完成基础实训（至少 1 个基础实训项目全部关卡通过）。未满足时页面展示提示卡 + 引导按钮「去完成基础实训」（跳 `/map`），不展示岗位选择操作。

**页面结构**

1. 标题「选择你的目标岗位」+ 副标题「选择一个你感兴趣的岗位，开始针对性的学习和实训」。
2. 岗位卡片网格（3 列）：岗位图标、岗位名称、岗位描述、难度、热度、已完成项目数、等级。推荐岗位卡片高亮显示 **匹配度百分比**（按技能树完成情况自动计算，匹配度高的排前面）。
3. 筛选：按方向 / 难度 / 热度筛选，支持浏览全部岗位。
4. 底部操作条：「取消」+「确认选择」。

**交互**

- 点击卡片选中（蓝框 + 右上角 ✓），同一时间只能选中一个。
- 「确认选择」→ 二次确认弹窗（提示「切换后学习进度保留」）→ 写入当前岗位 → 跳转成长中心，「我的岗位」卡片同步更新。
- 已是当前岗位时按钮显示「当前岗位」且禁用。

**验收标准**

- [ ] 未达成前置条件时显示引导页，不可选择。
- [ ] 推荐岗位按匹配度排序并展示百分比。
- [ ] 选择后成长中心「我的岗位」与关卡地图默认筛选同步更新。
- [ ] 切换岗位后原岗位进度不丢失。

---

### 6.9 认证中心（P0 入口 + 占位）

路由：`/certification`。对应原型 image38 / image39。

需求优先级说明：「主导航 - 认证中心」为 P0 入口；「技能鉴定」为 P2、「技能鉴定证书」为 P1。因此本期 P0 交付 **页面骨架 + 真实数据展示（Mock）**，不实现证书生成/下载。

**页面结构**

1. 标题「技能鉴定」+ 副标题「完成认证条件，获得技能徽章与称号」。
2. 认证条件三卡：基础项目完成（已完成 2/5）、进阶项目完成（进行中 2/4）、技能树达标（已掌握 8/12），每卡显示图标、名称、要求说明、当前进度；已达成显示 ✓ 绿色。
3. 状态页签：已获得 / 审核中 / 未达标。
4. 证书列表项：岗位图标 + 岗位名 + 获得时间 + 状态标签（已获得 / 审核中 / 未达标）。
5. 「查看技能鉴定书」按钮（P1，本期点击提示「功能开发中」或展示静态证件预览弹窗）。
6. 技能鉴定书样式（P1 预留）：深蓝底证件卡，含姓名、学号（脱敏）、完成率、各体系掌握数据、技能徽章墙、认证机构与证书编号。

**验收标准**

- [ ] 三个认证条件卡进度与 Mock 数据一致，达成态正确。
- [ ] 页签切换过滤列表正确。
- [ ] P1 按钮有明确占位反馈，不出现死链接。

---

## 7. P1 / P2 规划（本轮不做，仅登记）

| 优先级 | 功能 | 说明 |
| --- | --- | --- |
| P1 | Token 刷新 | 前端自动用 Key 换新 Token，对用户透明；Key 过期则回登录页 |
| P1 | 实训进度概览（增强） | 层级进度下钻、趋势图 |
| P1 | 历史记录 | 全部历史闯关与提交记录列表；重新挑战已完项目并覆盖成绩 |
| P1 | 技能鉴定证书 | 生成技能鉴定书并支持查看/下载 |
| P2 | 技能鉴定 | 完整认证条件判定与徽章体系 |
| P2 | 证书申领流转 | 申请、审核中、未达标全流程展示 |
| P1 | 404 兜底页 | 路由兜底 |

## 8. 非功能需求（前端相关）

| 项 | 要求 | 对应文档 |
| --- | --- | --- |
| 页面响应 | 非大模型接口 P95 ≤ 500ms / P99 ≤ 1000ms；前端需路由懒加载、接口并发请求、图片懒加载、列表虚拟滚动（>100 条时） | 3.1 性能 P0 |
| 并发能力 | 前端不阻塞：请求去重、失败重试退避、AI 问答流式/异步加载不卡 UI | 3.1 并发 P0 |
| 安全 | Token 存于 `localStorage` 并统一注入；所有用户输入（作答、问答）做前端校验与转义展示（默认插值，禁用 `v-html` 渲染用户内容）防 XSS；接口限流提示友好化 | 3.3 安全 P0 |
| 可用性 | 接口异常时页面不崩，展示重试；关键操作二次确认 | 3.2 P0 |
| 可维护性 | 导航、技能体系、认证条件、关卡模块均配置化；类型全覆盖；`vue-tsc` 零错误 | — |
| 浏览器 | Chrome / Edge 最新两个大版本；适配 1440px 与 1920px 宽，最小支持 1280px | — |
| 视觉 | 与原型图 image28–image39 保持一致：顶栏蓝色渐变、卡片圆角、状态配色统一 | — |

## 9. 技术方案与目录结构

**技术栈**（依据需求文档 1.3.2 技术架构路线）：Vue 3 + Vite + TypeScript + Composition API、Pinia、Vue Router、Element Plus、ECharts（P1 图表）。

```
Student/
├─ PRD.md
├─ index.html
├─ vite.config.ts
├─ package.json
└─ src/
   ├─ main.ts
   ├─ App.vue
   ├─ assets/styles/         # 设计变量、全局样式
   ├─ config/nav.ts          # 主导航配置
   ├─ layouts/DefaultLayout.vue
   ├─ components/
   │  ├─ AppHeader.vue
   │  ├─ AppNav.vue
   │  ├─ PageTitle.vue
   │  ├─ PositionCard.vue
   │  ├─ SkillNode.vue
   │  ├─ ProjectCard.vue
   │  ├─ LevelStepper.vue
   │  ├─ AiReviewPanel.vue
   │  ├─ AiAssistantDock.vue
   │  └─ EmptyState.vue
   ├─ views/
   │  ├─ LoginView.vue
   │  ├─ GrowthCenterView.vue
   │  ├─ SkillTreeView.vue
   │  ├─ LevelMapView.vue
   │  ├─ ProjectLevelView.vue
   │  ├─ PositionSelectView.vue
   │  └─ CertificationView.vue
   ├─ router/index.ts
   ├─ stores/                # user / skill / project / position / chat
   ├─ api/                   # 接口层（含 mock 实现开关）
   ├─ mock/                  # 固定 seed 的假数据
   ├─ types/                 # 第 5 节全部类型
   └─ utils/                 # request、format、storage
```

## 10. 开发计划（P0 优先）

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| M1 | 工程初始化：路由、Pinia、Element Plus、设计变量、类型定义、Mock 数据 | 可跑通空框架 + 数据 |
| M2 | 全局框架：顶部信息条、主导航、默认布局、登录页 | 所有页面外壳 |
| M3 | 成长中心（我的岗位 + 快捷入口 + 进度概览 + AI 助教浮窗） | 首页可用 |
| M4 | 技能树（四体系 + 节点状态 + 详情卡 + 跳转） | 技能树可用 |
| M5 | 关卡地图（岗位筛选 + 层级分组 + 四态卡片） | 地图可用 |
| M6 | 关卡详情（七步步骤条 + 作答 + 上传 + 提交 + AI 判分 + 申请复评） | 主链路闭环 |
| M7 | 岗位选择（前置校验 + 推荐排序 + 选择切换） | 岗位链路闭环 |
| M8 | 认证中心 + AI 问答完善 + 联调打磨（骨架屏/空态/错误态/响应式） | P0 全量交付 |

## 11. P0 验收清单

- [ ] 五大一级入口均可用，路由切换不刷新页面，高亮正确。
- [ ] 顶部信息条展示头像、姓名、专业、班级、累计积分、实训等级、完成率、称号。
- [ ] 成长中心：我的岗位按点亮技能排序、卡片字段齐全、可查看岗位技能树并跳转未点亮技能；四个快捷入口跳转正确。
- [ ] 技能树：四大体系分列、四类节点状态、点亮进度角标、节点详情卡、未解锁节点「前往实训」跳转。
- [ ] 关卡地图：岗位筛选、三层级分组、四态关卡卡片、未解锁提示、已完成显示分数。
- [ ] 关卡详情：七步步骤条、任务描述/验收标准/历史记录/教师点评、文本 + 附件提交、前置关卡校验、草稿保存。
- [ ] AI 判分：总分 + 等级 + 各维度理由 + 提升建议；通过解锁下一关；申请教师复评状态流转正确。
- [ ] AI 助教：上下文提示、来源标注、长度/次数/历史/三轮上下文限制、失败重试。
- [ ] 岗位选择：前置条件校验、匹配度排序、确认选择二次确认、切换后进度保留且成长中心同步。
- [ ] 认证中心：三个认证条件卡 + 三状态页签 + 证书列表，P1 能力有明确占位反馈。
- [ ] 质量：`npm run build` 与 `npm run type-check` 通过；无控制台报错；页面在 1440/1920 宽度下布局正常。

---

## 12. 待确认问题（Open Questions）

| 编号 | 问题 | 影响 | 建议默认 |
| --- | --- | --- | --- |
| Q1 | 主导航顺序：文档为「成长中心 / 技能树 / 关卡地图 / 岗位选择 / 认证中心」，原型图为「成长中心 / 岗位选择 / 关卡地图 / 技能树 / 认证中心」 | 全局导航 | 暂按文档顺序，配置化一处可改 |
| Q2 | 技能树右上角统计面板（已激活/已精通/技能总数）是否需要保留（文档批注建议去掉） | 技能树页面 | 保留但做成开关 |
| Q3 | 关卡状态三态（文档）还是四态（原型含「可挑战」） | 关卡地图 | 采用四态 |
| Q4 | 技能树第四体系名称：文档为「系统部署系」，原型为「项目部署系」 | 技能树文案 | 暂按原型「项目部署系」 |
| Q5 | 学生端是否需要「重新挑战并覆盖成绩」入口的明确提示文案 | 关卡详情 | 本期按「复习该项目」入口处理 |
| Q6 | AI 问答的每日次数上限与单条字数上限具体数值 | AI 助教 | 暂定 500 字 / 每日 50 次，配置化 |
